<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/iconapp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconapp"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dexlibrary"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".presentation.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Dexlibrary.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.LogInActivity" android:theme="@style/Theme.Dexlibrary.NoActionBar" />
        <activity android:name=".presentation.RegActivity" android:theme="@style/Theme.Dexlibrary.NoActionBar" />
        <activity
            android:name=".presentation.MainActivity"
            android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>